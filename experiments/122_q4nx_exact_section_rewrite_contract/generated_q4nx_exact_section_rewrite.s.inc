// Generated by exp122 from the exp120 section contract and exp121 numeric gate.
// This include is a rewrite contract, not production assembly.
// Each section removes one MyLM zero-correction MAC per logical group.

.macro Q4NX_EXACT_FILL
	// section fill
	// source_macs_per_group=28
	// exact_macs_per_group=27
	// removed_zero_macs_per_group=1
	// live_in=entry
	// live_out=boundary1
	// generator must emit exact_recompose_coeff before each MAC
.endm

.macro Q4NX_EXACT_FILL_TO_STEADY
	// section fill_to_steady
	// source_macs_per_group=33
	// exact_macs_per_group=32
	// removed_zero_macs_per_group=1
	// live_in=boundary1
	// live_out=boundary2
	// generator must emit exact_recompose_coeff before each MAC
.endm

.macro Q4NX_EXACT_STEADY_TO_STEADY
	// section steady_to_steady
	// source_macs_per_group=33
	// exact_macs_per_group=32
	// removed_zero_macs_per_group=1
	// live_in=boundary2
	// live_out=boundary6
	// generator must emit exact_recompose_coeff before each MAC
.endm

.macro Q4NX_EXACT_PRE_DRAIN
	// section pre_drain
	// source_macs_per_group=33
	// exact_macs_per_group=32
	// removed_zero_macs_per_group=1
	// live_in=boundary6
	// live_out=boundary7
	// generator must emit exact_recompose_coeff before each MAC
.endm

.macro Q4NX_EXACT_DRAIN
	// section drain
	// source_macs_per_group=38
	// exact_macs_per_group=37
	// removed_zero_macs_per_group=1
	// live_in=boundary7
	// live_out=exit
	// generator must emit exact_recompose_coeff before each MAC
.endm

.macro Q4NX_EXACT_SECTION_REWRITE_BODY
	// group0
	Q4NX_EXACT_FILL
	// group1
	Q4NX_EXACT_FILL_TO_STEADY
	// group2
	Q4NX_EXACT_STEADY_TO_STEADY
	// group3
	Q4NX_EXACT_STEADY_TO_STEADY
	// group4
	Q4NX_EXACT_STEADY_TO_STEADY
	// group5
	Q4NX_EXACT_STEADY_TO_STEADY
	// group6
	Q4NX_EXACT_PRE_DRAIN
	// group7
	Q4NX_EXACT_DRAIN
.endm
