# Q4NX MIR Opcode Coverage Map

This experiment checks whether the MyLM group1 Q4NX instruction vocabulary can
be expressed as AIE2P machine MIR and compiled by Peano `llc`.

It is intentionally a compiler/backend experiment, not a NPU numeric test.

