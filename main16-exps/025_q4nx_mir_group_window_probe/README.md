# Q4NX MIR Group Window Probe

This experiment extends the direct AIE2P MIR route from a single MyLM group to
small steady-state group windows.

It checks whether the compiler route can preserve the MyLM instruction shape
across group boundaries before attempting a numeric NPU replacement.

